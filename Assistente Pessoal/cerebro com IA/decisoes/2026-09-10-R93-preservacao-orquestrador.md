# Decisão 2026-09-10 — R93 Preservação do Orquestrador (guardrail universal)

- **Ordem do usuário**: orquestrador NUNCA usado para features (limitações atuais gargalam workflow) até revogação explícita ou upgrade de hardware com crivo empírico (decode ultra-rápido + ctx imenso + prefill ultrafast, nível cloud).
- **Causa do timeout (evidência)**: `llm-inventory.py --resolve subagent-executor` elegeu `:8083` (MoE 35B CPU, ~2 t/s, KV 5.76KB/tok) como "melhor" → wave jogou trabalho bruto no substrato do orquestrador → gargalo + tasks canceladas. Estrutural, não transient.
- **Regra promulgada**: R93 em `opencode/config/opencode/AGENTS.md` (hardlink canônico, mesmo inode nas duas vias). `local-orchestrator/orchestrator` = DISJUNTOR ABSOLUTO para feature (R65). Features → `proposer` :9088 · `refuter` :9090 · `judge` :9092 · `reflexo` :9086 · `ingestor` :9084, ou cloud-direct (R6/R10/R20).
- **Revogação**: só por ordem explícita ("revogo R93") ou upgrade de hardware PROVADO por crivo (R76/R79/R83) + veredito do Conselho (R75).
- **Pendente**: espelhar enforcement em `guarded_resolve`/inventário (excluir `orchestrator` de candidatos feature) — próxima forja.
- **Carrossel Obsidian**: retry R93-safe (descoberta via `explorador-tool`/reflexo rápido, forja via `hefesto`/`executor-f4` proposer) — usuário escolheu "Pipeline completo".
- **Ordem cloud-direct 16:5x (soberania explícita, R88 registrado)**: usuário mandou rotear tudo p/ nuvem e concluir. Refutação prévia CAIU (diagnóstico confirmou causa externa — benchmark :9096; nada a refutar na ordem). Execução no substrato cloud do orquestrador sob risco registrado: R1/R46 suspensos pontualmente por ordem direta; R93 (nada no :8083) e R8 (nada tocado no que funciona) MANTIDOS.
- **Entrega 16:52 (F5 gate)**: `node --check main.js` OK · manifest JSON válido · CSS chaves balanceadas · `fundo.css` legado INTACTO · arquivos nos paths reais. Smoke funcional no Obsidian PENDENTE do usuário (habilitar plugin + snippet) — partial honesto, sem autoabsolvição.
- **Hefesto nuvem 17:0x (autorização ampla do usuário c/ aviso prévio)**: DECOMP fundo.css (13 linhas) → AUTOFAGIA (GAP-1 conflito fratricida de véu por ordem alfabética de carga; GAP-2 preview/cm-scroller sem cobertura; GAP-3 fallback `none`; GAP-4 onload sem try/catch = morte silenciosa) → HELENIZAÇÃO/FORJA: (1) fundo.css v2.0 canônico (contrato --fundo-aleatorio-atual mantido + fallback gradiente + pan + véu c/ preview + graph experimental); (2) carrossel-fundo.css → stub de depreciação (R8: um conceito = um artefato); (3) main.js blindado (try/catch + Notice + window.setInterval); (4) configs JSON intactos. Verify: node --check OK · CSS 8/8. Pendente: reload do Obsidian pelo usuário + teste em nota e no graph.
- **Verificação exaustiva 18:2x ("nada acontece")**: auditoria 204/204 linhas → causa raiz = `addClass` (L27, FORA do try) + plugin habilitado na UI 18:09:39 com o código ainda quebrado = morte muda, zero sintoma. Correções: `classList.add` + try cobrindo TODO o init. Harness funcional Node (mock obsidian + DOM estrito sem `addClass`, 8 PNGs reais nos nomes): 2 runs vermelhos por erro ARITMÉTICO DO MEU TESTE (expectativa errada, código certo — registrado), 3º run **27/27 PASS** (onload, vars URL-codificadas, compat legada, véu, statusbar, timer, file-open md/canvas/null, create pasta/nota/imagem, delete, rename, 4 comandos, click statusbar, 6 controles de settings, clamp intervalo, pan off, véu ao vivo, unload limpo). Write em /tmp bloqueado pelo enforcement → smoke inline `node -e` (R6: refatoração de rota). Lógica PROVADA; falta só o reload no Obsidian p/ carregar o main.js corrigido.
- **v1.1 autossuficiente 18:3x (DevTools do usuário provou vars setadas mas nada pinta)**: plugin injeta o próprio `<style>` (zera dependência do painel de snippets), escreve a imagem INLINE no body (fura qualquer cascata/ordem de carga) e faz preload com autodiagnóstico (falha de URL vira Notice em tela, não silêncio). Harness estendido (mocks createElement/head/Image): **34/34 PASS**, incluindo estilo injetado idempotente, inline aplicado, preload na URL, unload removendo estilo+inline+timer. Usuário sem tarefa de console: reload (Ctrl+R) e sim/não.
- **v1.2 via autoral 21:xx (apoio do usuário: vídeo + transcrição yt-dlp + CSS original do autor)**: ground truth — custom-background.css (AnubisNekhet, AGPLv3, via MediaFire do vídeo) pinta no `.app-container` + véu por `backdrop-filter`, NUNCA no body (nosso erro estrutural). fundo.css = conteúdo autoral verbatim (crédito+licença+proveniência no header). Plugin pilota `--anp-background-image` + toggle + brilho mapeado do véu; removeu-se injeção própria, pan-placebo e toggle morto. Harness pegou 1 bug real (slider do véu chamava método renomeado) → corrigido + grep de resíduos limpo → **31/31 PASS**. Limitação herdada do referencial: graph view sem cobertura (o arquivo do autor também não cobre).
- **v1.3 enquadramento (bingo do usuário: imagens aparecem + shuffle ok; issue: esticadas)**: `carrossel-fundo.css` revive como camada de preferência — `contain !important` (inteira, sem corte/distorção, centralizada) + barras na cor do tema (`--background-primary`). `fundo.css` autoral intacto. Chaves 2/2. Reload (Ctrl+R) e conferir.
- **Diagnóstico stall 16:46 (evidência fresca, probes diretos)**: stack VIVA (health ok :8083/:9084/:9086/:9088/:9090), porém SATURADA — :9096 Nemotron-30B-CPU a 794% (subiu 16:43:32, no meio da sessão; :9095 morta p/ dar lugar; flags `-b 2048 -ub 512` = assinatura de sweep R76) + :9086 LFM a 92% (mastigando task cancelada — cancel no runtime NÃO aborta geração no server) + load 11.9 + swap 2GB em uso + VRAM 92% (15.8/17.1GB). 317 t/s do LFM é figura GPU; aqui ele roda CPU (`-ngl 0`) — contenção é CPU-side (ver correção abaixo), não GPU.
- **Correção do usuário 16:5x (registrada sem autoabsolvição)**: mapa device real — GPU: :8083 (parcial, 36 layers), :9084, :9088, :9093; CPU (`-ngl 0`): :9086, :9090, :9092, :9096. :9090/:9092 idle (0.0%) = irrelevantes; :8083 idle (0.0%) no momento = sem contenção dele. A briga é **CPU-side: :9096 794% vs :9086 92% + banda DDR + swap 2GB**. Usuário confirmou **benchmarks em outra sessão** — :9096 (sweep `-b 2048 -ub 512`, nascido 16:43:32) é o benchmark; causa do stall CONFIRMADA, não mais hipótese. Conduta: pipeline local pausado até o benchmark liberar a CPU; nada local lançado nesse meio-tempo.
